<%= form_with(model: carrito, local: true) do |form| %>
  <% if carrito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrito.errors.count, "error") %> prohibited this carrito from being saved:</h2>

      <ul>
      <% carrito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cantidad_productos %>
    <%= form.number_field :cantidad_productos, id: :carrito_cantidad_productos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
